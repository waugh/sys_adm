# Experimental.

# sudo apt-get install -y unionfs-fuse # Seems to work fine.

# sudo apt-get install -y transmission-daemon # Never figured out how to config.

sudo apt-get install -y qprint # Interprets quoted-printable MIME type.

sudo apt-get install -y mpack # mail unpack, munpack.
